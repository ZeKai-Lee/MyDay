# MyDay · 开发计划 v1.0

- 依据：[PRD.md](PRD.md) v1.0（2026-09-18 定稿）
- 执行方式：按里程碑顺序实现，每个里程碑完成即自查并汇报；全程只产出本地文件，不联网、不装依赖。
- 配套：[页面结构与交互设计.md](页面结构与交互设计.md)（线框图）

---

## 1. 技术方案（定案，开发中不再改动）

| 项 | 决定 |
|---|---|
| 产物 | **单个 `MyDay.html`**：CSS、JS 全部内嵌；零外部依赖（无 CDN、无网络字体、无图标库，图标用内嵌 SVG） |
| 代码方式 | 原生 JS，无框架、无构建步骤、不用 ES module；文件内用清晰的分区注释组织 |
| 渲染 | JS 渲染函数生成各模块视图 + 事件委托（data-action）；语言/主题/数据变化 = 重渲染当前视图 |
| 路由 | 内存中的当前板块变量（**刷新回首页**，符合 PRD，不用 hash 路由） |
| 存储 | localStorage 单键 `myday-data-v1`，整体 JSON（结构见第 4 节，含 `schemaVersion: 1`）；任何改动立即写入 |
| 导出/导入 | 导出 = Blob + download 属性；导入 = `<input type="file">` + FileReader + 结构校验 |
| 日期 | 一律 `YYYY-MM-DD` 本地日期字符串；自写格式化函数，**禁用 toISOString**（UTC 偏移坑） |
| 主题 | CSS 变量两套令牌（浅/深），`:root[data-theme="dark"]` 切换 |
| 安全 | 所有用户文本渲染前 HTML 转义；中文输入法回车用 `isComposing` 过滤，避免选字误提交 |
| 浏览器目标 | 桌面版 Chrome / Edge（Chromium），不考虑其他内核 |

## 2. 里程碑总览

| # | 里程碑 | 依赖 | 完成标志 |
|---|---|---|---|
| M1 | 地基：外壳 + 数据层 + 双语 + 主题 + 全局组件 | — | 断网打开可切换 9 个空板块；语言/主题切换并重启保持 |
| M2 | 今日计划 + 首页（速览卡、快速备忘） | M1 | 首页与今日计划实时联动；顺延机制正确 |
| M3 | 自媒体 | M1 | 看板流转、统计、首页摘要卡点亮 |
| M4 | 开发工作 | M1 | 项目/任务/笔记/搜索可用，首页摘要卡点亮 |
| M5 | 咨询工作 | M2 | 客户/记录可用，待办一键进今日计划 |
| M6 | 健身计划 | M1 | 模板/打卡/历史/统计可用 |
| M7 | 饮食计划 | M1 | 四餐/喝水/速查表可用 |
| M8 | 游戏娱乐 | M1 | 游戏库/时长/统计可用 |
| M9 | 数据与设置 + 备份提醒 | M1~M8 | 导出/导入/清空/提醒状态全部可用 |
| M10 | 全量验收 + 打磨 + 交付 | 全部 | PRD 17 条验收全过，交付说明完成 |

说明：首页六张摘要卡在 M2 搭好骨架，M3~M8 每完成一个模块就点亮对应卡片；M9 放最后是因为备份导入导出要在数据结构全部稳定后做。

## 3. 各里程碑任务清单

### M1 地基（约占总工作量 1/4，是后面一切的地基）
1. `MyDay.html` 骨架：标题 MyDay，`<style>` 与 `<script>` 大分区注释（令牌→布局→组件→模块 → 字典→存储→工具→渲染→各模块→启动）。
2. 设计令牌：浅/深两套（背景、卡片、文字三级、主色、四归属色、状态色）。
3. 布局：侧栏（MyDay + 本地日期星期、9 项导航、底部数据状态区占位）+ 内容区；当前项高亮。
4. 存储层：首次运行播种空结构（默认 `lang: zh, theme: light, lastExportAt: null`）；load/save/uid/日期工具/escapeHtml。
5. i18n：zh/en 字典（导航、通用词、枚举名）；`t()`；切换语言即重渲染。
6. 全局组件：行内添加、行内编辑（Enter/Esc）、删除+撤销 toast（5 秒）、确认弹窗（含"输入清空"强确认变体）、空状态、已完成沉底+显示开关、页签。

自查：断网双击打开；9 项可切换（空视图+空状态文案）；切语言/主题立即生效，关浏览器重开保持；刷新回首页。
（覆盖验收：1、3、12、13、17 的基础）

### M2 今日计划 + 首页核心
1. 今日待办：增（选归属，默认生活）、勾、改、删（可撤销）；四组分组；已完成沉底+开关。
2. 「之前未完成」折叠区：`date < 今天 且未完成` 的条目；操作【加入今天=改日期 / 完成掉 / 删除】。
3. 首页：日期问候；速览卡（进度条、分组列表、勾选、快速添加）；快速备忘独立数据增删划。
自查：AC 2、4、5、6；改系统日期跨天行为正确。

### M3 自媒体
1. 灵感库：增、删、删除可撤销；「转成正式内容」→ 进入看板"想法"列，同时从灵感库移除（避免重复）。
2. 看板四列；卡片显示标题/平台/日期；「推进→」逐列前进（已发布列无此按钮）；编辑表单（标题/平台/日期/备注/直接选阶段）；删除可撤销。
3. 已发布列默认最近 5 条 + 查看更多。
4. 统计条：本月已发布数 + 平台分布。
5. 首页自媒体摘要卡点亮。
自查：AC 7；首页卡片数字与看板一致。

### M4 开发工作
1. 左项目列表（名称/状态/任务进度 n/m）+ 新建；右详情：改名、状态切换。
2. 删除项目：确认弹窗，连带删除其任务与笔记。
3. 项目任务清单（按全局约定）；笔记增删改（标题+多行内容）。
4. 顶部搜索：跨项目搜笔记标题/内容 → 右列切结果列表 → 点击跳转。
5. 首页开发摘要卡点亮。
自查：删除连带清理无残留；搜索与跳转正确。

### M5 咨询工作（依赖 M2）
1. 客户列表+搜索；新建/行内编辑/删除（确认，连带记录）。
2. 详情：基本信息（备注/联系方式）行内编辑；咨询记录添加（日期默认今天/摘要/时长分钟/后续待办）、倒序、删除可撤销。
3. 「后续待办 → 加入今日计划」：生成归属=咨询的待办；`todoAdded` 标记防重复，按钮变"已加入"。
4. 统计：本月咨询次数、累计时长。
5. 首页咨询摘要卡点亮。
自查：AC 8；与今日计划联动、防重复正确。

### M6 健身计划
1. 页签：今日训练 / 每周模板 / 历史记录。
2. 模板：周一~周日七行点击改文字，留空=休息。
3. 今日训练：显示当天模板主题或"今天休息"；动作条目记录（名/组/次/重量/备注，可多条）；补记其他日期。
4. 历史：按日期倒序、展开明细；统计本月/本周次数（**周一起始**）。
5. 首页健身摘要卡点亮。
自查：AC 9；历史"主题"列取该日期对应模板文字。

### M7 饮食计划
1. 日期切换 ‹ 昨天 · 今天 ›（往回补记）。
2. 四餐卡：行内添加（食物名+可选千卡）、删除；按日期隔离。
3. 喝水卡：+1/−1 杯；目标杯数设置（默认 8）。
4. 速查表页签：表格增删；餐卡「从速查表选」→ 筛选 → 点选自动带入名称+热量。
5. 今日总热量小计。
6. 首页饮食摘要卡点亮。
自查：AC 10；切日期后各餐与喝水数据不串。

### M8 游戏娱乐
1. 游戏添加（默认"想玩"）/删除（可撤销）/状态切换；筛选 chips（全部/在玩/想玩/通关/弃坑）。
2. 「记录游玩」输入分钟 → 累计时长 + 游玩记录。
3. 最近游玩列表（倒序最多 10 条）。
4. 本月总时长统计。
5. 首页游戏摘要卡点亮。
自查：AC 11。

### M9 数据与设置 + 备份提醒
1. 语言、主题设置项（与 M1 联调）。
2. 导出：下载 `MyDay-YYYY-MM-DD.json`；写 `lastExportAt`。
3. 导入：校验（JSON 可解析、结构完整、schemaVersion 一致）→ 确认框（显示各模块数据量，提示覆盖）→ 覆盖并重渲染。
4. 清空全部：手动输入"清空"才能执行。
5. 说明区：数据存放位置、定期备份建议、版本号。
6. 侧栏备份提醒：从未导出/>30 天红、7~30 天黄、<7 天绿。
自查：AC 14、15、16。

### M10 全量验收与交付
1. 按第 5 节方式走查 PRD 17 条验收，逐条记录结果。
2. 边界与性能抽查：超长文本显示；造 200 条待办 + 100 条记录验证流畅。
3. 打磨：添加后焦点回输入框、Enter/Esc 全场景一致、层级字号统一。
4. 交付：使用说明（双击打开、备份建议、注意事项）；PRD 标注交付状态。

## 4. 数据结构定义（开发用，localStorage 键 `myday-data-v1`）

```jsonc
{
  "schemaVersion": 1,
  "prefs": { "lang": "zh", "theme": "light", "lastExportAt": null },

  "home":  { "memos": [ { "id": "", "text": "", "done": false } ] },

  "today": { "todos": [ { "id": "", "text": "", "tag": "life|media|dev|consult",
                          "date": "YYYY-MM-DD", "done": false } ] },

  "media": {
    "ideas": [ { "id": "", "text": "", "createdAt": "YYYY-MM-DD" } ],
    "items": [ { "id": "", "title": "", "stage": "idea|wip|draft|published",
                 "platform": "", "date": "YYYY-MM-DD 或 null", "note": "" } ]
  },

  "dev": {
    "projects": [ { "id": "", "name": "", "status": "active|paused|done", "createdAt": "" } ],
    "tasks":    [ { "id": "", "projectId": "", "text": "", "done": false } ],
    "notes":    [ { "id": "", "projectId": "", "title": "", "content": "" } ]
  },

  "consult": {
    "clients": [ { "id": "", "name": "", "note": "", "contact": "" } ],
    "sessions": [ { "id": "", "clientId": "", "date": "YYYY-MM-DD", "summary": "",
                    "minutes": null, "followUp": "", "todoAdded": false } ]
  },

  "fitness": {
    "weekTemplate": { "mon": "", "tue": "", "wed": "", "thu": "", "fri": "", "sat": "", "sun": "" },
    "logs": [ { "id": "", "date": "YYYY-MM-DD",
                "exercises": [ { "name": "", "sets": null, "reps": null, "weightKg": null, "note": "" } ] } ]
  },

  "diet": {
    "days": { "YYYY-MM-DD": {
      "meals": { "breakfast": [ { "id": "", "name": "", "kcal": null } ],
                 "lunch": [], "dinner": [], "snack": [] },
      "water": 0 } },
    "waterGoal": 8,
    "foodTable": [ { "id": "", "name": "", "kcal": null } ]
  },

  "games": {
    "games": [ { "id": "", "name": "", "status": "playing|wish|finished|dropped", "totalMinutes": 0 } ],
    "plays": [ { "id": "", "gameId": "", "date": "YYYY-MM-DD", "minutes": 0 } ]
  }
}
```

约定：所有枚举只存键，显示时经 `t()` 按当前语言渲染；统计数字（本月发布数、训练次数等）一律渲染时实时计算，不落库；`diet.days` 只存有数据的日期。

## 5. 验收执行方式（对应 PRD 第 10 节 17 条）

| 验收条目 | 内容摘要 | 检查方式 |
|---|---|---|
| 1 | 断网可用 | 手工：断网后双击打开全功能走一遍 |
| 2 | 关浏览器数据不丢 | 手工：存数据→完全关闭→重开 |
| 3 | 刷新不丢 | 浏览器自动化：刷新后断言数据 |
| 4 | 跨天顺延 | 手工：改系统日期到次日 |
| 5、6 | 首页↔今日计划联动；归属/撤销 | 自动化：点击+断言 |
| 7 | 自媒体看板流转与统计 | 自动化 |
| 8 | 咨询待办进今日计划、防重复 | 自动化 |
| 9 | 健身模板/打卡/统计 | 自动化 |
| 10 | 饮食记录/速查表/喝水 | 自动化 |
| 11 | 游戏时长累计/筛选 | 自动化 |
| 12、13 | 双语切换、枚举不错乱、原文保留 | 自动化（"重启保持"复用条目 2 结论） |
| 14 | 备份提醒红黄绿 | 自动+手工改日期验证黄色 |
| 15 | 导出→清空→导入恢复 | 手工为主（涉及下载文件与文件选择） |
| 16 | 坏文件导入不影响现有数据 | 手工（准备损坏 JSON） |
| 17 | 主题切换并记住 | 自动化+重开复核 |

自动化部分用浏览器自动化工具驱动页面实际点击验证并截图留证；手工部分在汇报中给出逐步操作说明。

## 6. 风险与实现注意点

1. **file:// 的存储是全机共享的**（Chromium 下所有本地页面同一个 localStorage 空间）：好处是移动/重命名 HTML 文件不丢数据；注意键名 `myday-data-v1` 保持唯一，避免和其他本地测试页冲突。
2. **清理浏览器数据会清掉 localStorage**：设置页必须写明，配合侧栏备份提醒。
3. **本地日期**：全部用自写格式化（年-月-日、周几按本地时区），禁用 `toISOString()`。
4. **中文输入法**：所有回车提交判断 `isComposing`，否则选字回车会误添加。
5. **HTML 转义**：用户输入一律转义后渲染，防止内容中 `<`、`<script>` 等破坏页面。
6. **导入校验**：解析失败/结构缺失/schemaVersion 不符 → 提示失败且不改动现有数据。
7. **单文件体积**：预计数千行，改动前先按分区注释定位，不做无序追加。
8. localStorage 容量约 5MB，纯文本数据远够用（第一版也不做图片附件）。

## 7. 交付物

1. `MyDay.html` —— 双击即用的成品。
2. 使用说明（写在设置页"说明区"+交付汇报里）：如何打开、如何导出备份、清理浏览器数据前先导出。
3. PRD 状态更新为"v1.0 已交付"。
