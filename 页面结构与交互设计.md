# 个人工作生活 App · 页面结构与交互设计（第一版）

> 形态：单个 HTML 文件，双击在浏览器打开；数据存这台电脑的浏览器里，支持导出/导入存档文件。
> 界面语言：**中文（默认）/ English，可随时切换**，切换立即生效并记住选择。
> 默认浅色主题，可切换深色。按电脑桌面浏览器设计（不考虑手机端）。
> App 名称：「MyDay」（中英文界面下都显示 MyDay）。

---

## 1. 全局框架

```
┌──────────────┬────────────────────────────────────────────┐
│ MyDay         │  首页总览                                    │
│ 9月18日 周五  ├────────────────────────────────────────────┤
│              │                                            │
│ ▣ 首页总览    │                                            │
│ ● 今日计划    │           内容区（当前模块的页面）            │
│ ◆ 自媒体      │                                            │
│ ⌨ 开发工作    │                                            │
│ ☎ 咨询工作    │                                            │
│ ✦ 健身计划    │                                            │
│ ☺ 饮食计划    │                                            │
│ ▤ 游戏娱乐    │                                            │
│ ⚙ 数据与设置  │                                            │
│              │                                            │
│ 数据已保存    │                                            │
│ 备份: 3天前   │                                            │
└──────────────┴────────────────────────────────────────────┘
```

（英文版所有标签对应替换，如：Home / Today's Plan / Content / Dev Work /
Consulting / Fitness / Diet / Games / Data & Settings，日期显示
"Fri, Sep 18"。）

- 左侧固定导航栏，宽约 210px：
  - 顶部：App 名称 + 今天日期星期；
  - 中部：9 个板块入口，当前所在板块高亮；
  - 底部：数据状态区 —— 「数据已保存」+「上次备份：n 天前」。
    不足 7 天显示绿色，7~30 天黄色，超过 30 天或从未导出显示红色；点击跳到「数据与设置」。
- 右侧内容区：每个模块自带标题行和快捷操作按钮。
- 刷新或重新打开后回到「首页总览」，不记忆上次停留位置，行为可预期。
- 所有改动即时自动保存，没有保存按钮。

## 2. 全局交互规则（所有模块统一）

1. **添加**：一律在列表顶部行内输入，回车或点「添加」，不用弹窗。
2. **编辑**：点击文字进入行内编辑，Enter 保存，Esc 取消。
3. **删除**：小项（待办、备忘、记录等）鼠标悬停出现删除按钮，点击即删，
   右下角弹「已删除 · 撤销」（5 秒内可撤销）；大项（开发项目、咨询客户、
   清空全部）弹确认框。
4. **完成类操作**：勾选后划线并沉到列表底部；列表较长时提供「显示/隐藏已完成」开关。
5. **空状态**：列表为空时显示一句引导文案，不留空白。
6. **日期**：以电脑本地日期为准；跨午夜后刷新或切换模块即显示新的一天。
7. **归属标签**：今日计划用固定四色标签（自媒体 / 开发 / 咨询 / 生活）。
8. **语言**：在「数据与设置」里一键切换 中文 / English，立即生效、无需刷新，
   选择被记住，下次打开保持。

## 3. 多语言设计

- **界面双语**：导航、按钮、标签、状态名、空状态提示、系统消息、
  日期格式、单位（杯/cups、千卡/kcal、分钟/min）全部双语。
- **切换方式**：设置页一键切换；默认中文；记住选择。
- **关键约定——固定枚举只存「键」**：归属（自媒体/开发/咨询/生活）、
  项目状态（进行中/暂停/完成）、内容阶段（想法/准备中/草稿完成/已发布）、
  游戏状态（在玩/想玩/通关/弃坑）等固定分类，数据里存的是内部键名，
  显示时按当前语言渲染。**切换语言不会让已有数据错乱。**
- **用户自己输入的内容不翻译**：任务名、笔记、咨询记录、食物名、
  自填的平台名等，永远保持你输入的原文。
- **导出存档与语言无关**：存档文件内容一致，可互相导入；存档文件名统一为
  MyDay-2026-09-18.json 格式（MyDay + 导出日期），两种语言下一致。

## 4. 各模块设计

### 4.1 首页总览

```
│ 9月18日 周五 · 下午好                              │
│ ┌─ 今日计划 4/9 ███████░░ ─────────────────────┐  │
│ │ + 输入事项，回车添加            [归属: 生活▾]   │  │
│ │ ☑ (自媒体) 剪今日视频                          │  │
│ │ ☐ (开发) 修登录bug                             │  │
│ │ ☐ (生活) 取快递                                │  │
│ └───────────────────────────────────────────────┘  │
│ ┌─ 快速备忘 ────────┐  ┌─ 自媒体 ─────────────┐    │
│ │ ▸ 记着交房租       │  │ 待发布: B站-xxx评测   │    │
│ │ ▸ 买HDMI线        │  │ 本月已发 5 条         │    │
│ └───────────────────┘  ├─ 健身 ────────────────┤    │
│                        │ 今天: 腿日 · 本周已练2次│    │
│                        ├─ 饮食 ────────────────┤    │
│                        │ 喝水 5/8杯 · 已记2餐   │    │
│                        └───────────────────────┘    │
```

- 顶部：日期 + 星期 + 按时段问候（早上好/下午好/晚上好）。
- **今日计划速览卡**（最显眼位置）：完成进度条；今日事项按归属分组显示，
  可直接勾选、可在首页直接快速添加；与「今日计划」模块数据实时同步。
- **快速备忘卡**：一行一条便签，回车即加；点击文字划掉（标记完成），悬停可删；不带日期。
- **模块摘要卡**：两列网格共 6 张，点击跳转对应模块：
  - 自媒体：最近一条待发布内容 + 本月已发布条数
  - 开发：进行中项目数 + 未完成任务数
  - 咨询：本月咨询次数 + 最近一次记录摘要
  - 健身：今天按模板练什么 + 本周已练次数
  - 饮食：喝水 x/y 杯 + 今日已记餐数和总热量
  - 游戏：在玩的游戏 + 本月总时长

### 4.2 今日计划

- 标题行：日期 + 完成度。
- 添加行：输入事项 + 归属选择（自媒体/开发/咨询/生活，默认「生活」），回车添加。
- 列表按归属分四组；每组内：勾选框 + 事项文字（点击编辑）+ 删除；已完成划线沉底。
- 顶部折叠区「之前未完成（n）」：列出昨天及更早未完成的事项，每条右侧三个操作：
  「加入今天」「完成掉」「删除」。
- 右上角「显示已完成」开关。

### 4.3 自媒体

```
│  本月已发布 5 条 · B站3 · 公众号2          [+ 新内容] │
│ ┌─ 想法 ─┐ ┌─ 准备中 ┐ ┌─ 草稿完成 ┐ ┌─ 已发布 ──┐ │
│ │  卡片   │ │  卡片    │ │  卡片     │ │ 卡片(日期) │ │
│ │ [推进→] │ │ [推进→]  │ │ [推进→]   │ │           │ │
│ └────────┘ └─────────┘ └──────────┘ └───────────┘ │
```

- 顶部统计条：本月已发布 n 条 · 各平台分布。
- 主体：四列看板「想法 → 准备中 → 草稿完成 → 已发布」。
  - 每张卡片：标题、平台、日期（已发布列显示发布日期）。
  - 卡片上「推进 →」按钮进入下一阶段；点击卡片展开编辑：
    标题 / 平台 / 日期 / 备注 / 直接选阶段。
  - 已发布列默认只显示最近 5 条，可「查看更多」。
- 左侧窄栏「选题灵感库」：纯快速输入列表；每条灵感旁「转成正式内容」
  按钮，一键送进看板「想法」列。
- 平台是自由文本，输入时给常用平台建议（B站、抖音、小红书、公众号等），可随意填。

### 4.4 开发工作

- 左列：项目卡片列表（名称、状态、任务完成 n/m）；「+ 新建项目」行内添加。
- 右侧：选中项目的详情：
  - 项目名（点击改名）、状态切换（进行中 / 暂停 / 完成）；
  - 任务清单：按全局规则增删改、勾选沉底；
  - 笔记与片段：该项目下的多条笔记（标题 + 内容），可添加、编辑、删除。
- 模块顶部搜索框：跨所有项目搜笔记标题和内容；搜索时右列变为结果列表，
  点击结果跳到对应项目。

### 4.5 咨询工作

- 顶部统计：本月咨询 n 次 · 累计时长 x 小时。
- 左列：客户列表 + 搜索框；「+ 新建客户」。
- 右侧客户详情：
  - 基本信息：备注、联系方式（点击行内编辑）；
  - 「＋ 添加咨询记录」：日期（默认今天）、内容摘要、时长分钟（选填）、后续待办（选填）；
  - 记录按日期倒序；每条记录的「后续待办」旁有「→ 加入今日计划」按钮，
    一键生成归属为「咨询」的待办。
- 删除客户需确认（会一并删除其咨询记录）。

### 4.6 健身计划

- 模块内三个页签：**今日训练 / 每周模板 / 历史记录**。
- 今日训练：
  - 顶部显示模板内容：「今天按计划：腿日」；当天没设模板显示「今天休息」。
  - 「记录一条动作」行内表单：动作名 + 组数 + 次数 + 重量kg（数字可留空）+ 备注；
    一天可记多条。
  - 「补记其他日期」：可改日期补记。
- 每周模板：周一到周日七行，每行一段文字（如「胸+三头」），点击即改；留空即休息。
- 历史记录：按日期倒序的训练日列表（日期、主题、动作条数），点击展开明细；
  顶部统计本月 / 本周训练次数。

### 4.7 饮食计划

- 顶部日期切换：‹ 昨天 · 今天 ›（可往回补记，默认今天）。
- 页签：**今日饮食 / 速查表**。
- 今日饮食：
  - 四张餐次卡：早餐 / 午餐 / 晚餐 / 加餐；每张卡顶部输入行「食物名称 + 可选热量(千卡)」，
    回车记录；条目可删；
  - 喝水卡：显示 n/目标 杯；「+一杯 / −一杯」；目标杯数可设置（默认 8）；
  - 今日小计：已记录总热量（填了才计）。
- 速查表：食物名 + 单份热量的表格，可增删；在餐次卡输入框旁「从速查表选」按钮，
  筛选后点选自动带入名称和热量。

### 4.8 游戏娱乐

- 顶部统计：本月总时长 · 在玩 n 款。
- 状态筛选 chips：全部 / 在玩 / 想玩（愿望单）/ 通关 / 弃坑。
- 游戏列表：名称、状态（点击可切换）、累计时长；每款游戏：
  - 「记录游玩」按钮 → 输入今天玩了多少分钟 → 自动累计并进入「最近游玩」。
- 「+ 添加游戏」行内输入，默认状态「想玩」。
- 底部「最近游玩」列表：日期 + 游戏 + 时长，倒序，最多显示 10 条。

### 4.9 数据与设置

- **语言**：中文 / English 一键切换，立即生效并记住。
- **外观**：浅色 / 深色切换，立即生效并记住。
- **数据备份**：
  - 「导出全部数据」：下载 JSON 存档文件，文件名含日期；导出后记录
    「上次备份时间」，驱动侧栏底部的备份提醒。
  - 「从存档导入」：选择文件 → 确认框（显示存档里的数据量，提示将覆盖
    当前全部数据）→ 覆盖导入。
- **危险区**：清空全部数据 —— 需手动输入「清空」两个字二次确认。
- **说明区**：数据存放位置说明（存在这台电脑的浏览器里，建议每月导出一次
  存档另存到U盘/网盘）、当前版本号。

## 5. 数据与保存机制（设计约定）

- 全部数据是一个整体存档对象，含九个模块各自的数据 + 界面语言、主题等偏好；
  任何增删改立即写入浏览器本地存储。
- 存档结构与导出文件、导入恢复完全一致，保证备份随时可用。
- 不联网、不发送任何数据。

## 6. 第一版明确不做

与需求确认一致：无提醒通知、无图表、无日历/月视图、无富文本和图片、
无周期自动任务、无多设备同步。

## 7. 确认记录

- 2026-09-18：需求与设计整体确认通过；App 名字定为 **MyDay**。
- 遗留小项：英文界面文字按日常习惯翻译，使用中发现别扭的地方随时提，改词库即可。
- 2026-09-18：**v1.0 开发完成并交付。** 自动化验收 236 条断言全部通过
  （tests/m1–m10）；3 条手工验收项（断网双击/关浏览器重开/改系统日期跨天）
  操作步骤见 tests/README.md。
- 2026-09-18：**v1.1 按使用反馈更新（6 项）：** ①整体视觉重设计（新配色、
  渐变主按钮、卡片悬浮、分段式页签、渐变进度条）②自媒体新增"数据看板"：
  已发布内容可录入播放/点赞/评论，自动生成对比条形图，可切换维度
  ③开发任务添加改为显式"+ 添加任务"按钮 ④咨询客户删除改为显式"删除客户"
  按钮（带确认）⑤健身/饮食各新增"月历"视图：每天显示部位+动作数 /
  热量+餐数+喝水，点击任意日期回到该天补记 ⑥侧栏新增"立即保存"按钮，
  支持 Ctrl/Cmd+S。全量回归 m1–m11 共 266 条断言全部通过。
