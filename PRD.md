# MyDay · 产品需求文档（PRD）v1.0

- 日期：2026-09-18　状态：**v1.1 已交付**（自动化验收 266 条断言全部通过，见 tests/）
- 配套文档：[页面结构与交互设计.md](页面结构与交互设计.md)（线框图与交互细节）
- 本文档是第一版开发的依据；与配套文档不一致时，以本文档为准。

---

## 1. 产品目标和使用场景

**定位**：只给需求方一个人使用的个人工作生活管理工具，名字 **MyDay**。

**目标**：
- 把每天的安排、在推进的工作、要保持的生活习惯放进同一个入口，打开 30 秒看清今天的状态；
- 每个板块提供贴合自身场景的记录功能，**不是九个换名字的待办清单**；
- 数据永远在自己电脑上，不依赖任何外部服务。

**使用场景**：
- 早上：打开首页看今天安排，快速补几条待办和备忘；
- 工作中：开发任务打勾；咨询结束记一条记录、把跟进事项放进今日计划；自媒体内容从灵感到发布流转状态；
- 生活中：练完记一笔，吃饭随手记、喝水打卡，玩完游戏记时长；
- 复盘：各模块自带简单统计（本月发布数、咨询时长、训练次数、游戏时长等）。

**硬性约束**：
- 仅本机单人使用：不登录、不联网、无云数据库、不需要部署、无手机端；
- 运行形态：**单个 HTML 文件，双击在浏览器打开**（Chrome / Edge）；
- 界面语言：中文（默认）/ English，可随时切换（见第 2、8 节）。

## 2. 整体页面框架和导航结构

- 单页应用：一个 HTML 文件内切换 9 个板块视图，无页面跳转加载、无路由刷新。
- 布局：左侧固定导航栏（宽约 210px）+ 右侧内容区。
  - 导航栏顶部：App 名 **MyDay** + 今天日期星期；
  - 中部：9 个入口，顺序固定：**首页总览、今日计划、自媒体、开发工作、咨询工作、健身计划、饮食计划、游戏娱乐、数据与设置**，当前项高亮；
  - 底部：数据状态区 —— 「数据已保存」+「上次备份：n 天前」（规则见第 7 节），点击跳转「数据与设置」。
- 刷新或重新打开后回到「首页总览」，不记忆上次停留位置。
- 主题：浅色（默认）/ 深色，设置里切换并记住。
- 所有改动**即时自动保存**，没有保存按钮。

**全局交互约定**（所有模块一致，第一版必须实现）：
1. 添加 = 列表顶部行内输入 + 回车，不用弹窗；
2. 编辑 = 点击文字就地编辑，Enter 保存、Esc 取消；
3. 删除 = 小项悬停出现删除按钮，点击即删，右下角弹「已删除 · 撤销」（5 秒内有效）；大项（开发项目、咨询客户、清空全部数据）弹确认框；
4. 完成类操作 = 划线并沉到列表底部；长列表提供「显示/隐藏已完成」开关；
5. 空列表显示一句引导文案，不留空白；
6. 日期以电脑本地日期为准；跨午夜后刷新或切换模块即进入新的一天；
7. 今日计划归属标签固定四色：自媒体 / 开发 / 咨询 / 生活。

## 3. 首页的内容结构

从上到下（线框图见配套文档）：

1. **顶部**：日期 + 星期 + 按时段问候（早上好/下午好/晚上好）。
2. **今日计划速览卡**（最显眼位置）：完成进度条（如 4/9）；今日待办按归属分组显示；可直接勾选完成、可直接快速添加（可选归属）；与「今日计划」模块数据实时同步。
3. **快速备忘卡**：独立便签列表，一行一条；回车即加、点击文字划掉（标记完成）、悬停删除；不带日期。
4. **模块摘要卡**：两列网格共 6 张，点击跳转对应模块——
   - 自媒体：最近一条待发布内容 + 本月已发布条数
   - 开发：进行中项目数 + 未完成任务数
   - 咨询：本月咨询次数 + 最近一次记录摘要
   - 健身：今天按模板练什么 + 本周已练次数
   - 饮食：喝水 x/y 杯 + 今日已记餐数和总热量
   - 游戏：在玩的游戏 + 本月总时长

## 4. 每个模块的用途

| 模块 | 用途（它解决什么问题） |
|---|---|
| 首页总览 | 每天打开的第一屏：快速看清今天的状态，顺手做最常用的记录 |
| 今日计划 | 管理"今天要做什么"，是全 App 唯一以天为单位的待办清单 |
| 自媒体 | 管内容创作流水线：灵感不丢、进度可控、发布有账可查 |
| 开发工作 | 管理多个开发项目的任务，沉淀笔记、命令和踩坑记录 |
| 咨询工作 | 管理客户名录、咨询历史和跟进事项，知道本月服务量 |
| 健身计划 | 按每周模板执行训练、留下训练记录，看见坚持情况 |
| 饮食计划 | 简单记录每天吃了什么、水喝够没有，常用食物热量随手查 |
| 游戏娱乐 | 管理游戏库和游玩时长，掌握并适度控制娱乐时间 |
| 数据与设置 | 界面语言与外观偏好；数据备份、恢复和清空等数据安全操作 |

## 5. 每个模块第一版最必要的数据和操作

**首页总览**
- 数据：仅拥有「快速备忘」列表（文字 + 完成状态）；其余内容实时读取其他模块，不另存数据。
- 操作：勾选今日待办、快速添加待办（选归属）、备忘的添加/划掉/删除、点击摘要卡跳转。

**今日计划**
- 数据：待办条目 { 文字、归属（4 个固定键之一）、日期、完成状态 }。
- 操作：添加（默认归属"生活"）、勾选/取消、点击改文字、删除（可撤销）、按归属分四组展示、历史未完成的【加入今天/完成掉/删除】、显示/隐藏已完成。

**自媒体**
- 数据：灵感条目 { 文字、创建日期 }；内容条目 { 标题、阶段（想法/准备中/草稿完成/已发布，存键）、平台（自由文本）、日期、备注 }。
- 操作：灵感增删、灵感「转成正式内容」（进看板"想法"列）、卡片新增/编辑（标题/平台/日期/备注/直接选阶段）/「推进→」/删除、本月发布数与平台分布统计、已发布列默认显示最近 5 条可展开。

**开发工作**
- 数据：项目 { 名称、状态（进行中/暂停/完成，存键）}；任务 { 文字、完成状态、所属项目 }；笔记 { 标题、内容、所属项目 }。
- 操作：新建/改名/删除项目（删除需确认，连带其任务与笔记）、切换项目状态、任务增删改勾、笔记增删改、跨所有项目按关键词搜索笔记。

**咨询工作**
- 数据：客户 { 名称、备注、联系方式 }；咨询记录 { 日期、内容摘要、时长分钟（选填）、后续待办（选填）、所属客户、是否已加入今日计划 }。
- 操作：新建/编辑/删除客户（删除需确认，连带其记录）、客户搜索、添加咨询记录（日期默认今天）、记录的「后续待办」一键「加入今日计划」（每条只允许一次，加入后按钮变"已加入"）、本月咨询次数与累计时长统计。

**健身计划**
- 数据：每周模板 { 周一~周日各一段文字，留空=休息 }；训练记录 { 日期、主题、动作条目列表 [{ 动作名、组数、次数、重量kg、备注 }] }。
- 操作：编辑模板七行、今日训练页显示当天模板主题、记录动作条目（可记多条）、补记其他日期、历史记录按日期倒序可展开、本月/本周训练次数统计。

**饮食计划**
- 数据：按日期的餐次条目 { 餐次（早/中/晚/加餐）、食物名、热量千卡（可选）}；喝水 { 日期、杯数、目标杯数（默认 8）}；速查表 { 食物名、单份热量 }。
- 操作：日期切到昨天补记、四餐各自行内添加/删除条目、从速查表点选自动带入名称和热量、喝水 +1/−1 杯、设置目标杯数、速查表增删、今日总热量小计。

**游戏娱乐**
- 数据：游戏 { 名称、状态（在玩/想玩/通关/弃坑，存键）、累计分钟 }；游玩记录 { 日期、游戏、分钟 }。
- 操作：添加游戏（默认"想玩"）、删除游戏（可撤销）、切换状态、按状态筛选、「记录游玩」输入分钟（自动累计并写入最近游玩）、最近游玩列表（倒序最多 10 条）、本月总时长统计。

**数据与设置**
- 数据：语言偏好、主题偏好、上次导出时间。
- 操作：切换中文/English、切换浅色/深色、导出全部数据、从存档导入（确认后覆盖）、清空全部数据（需手动输入"清空"二次确认）。

## 6. 首页、今日计划和各模块之间的关系

1. **今日计划是"每日待办"的唯一数据源**。首页速览卡只是它的另一个视图：在首页勾选、添加 = 直接修改今日计划的数据，两边实时一致。
2. **各模块管理自己的领域数据**，与今日计划的连接点只有一个：咨询记录的「后续待办」可一键生成归属=咨询的今日待办（每条只允许一次）。
3. **首页六张摘要卡是各模块数据的只读汇总**，实时计算显示，不另外存一份数据。
4. **首页「快速备忘」是首页自己的独立数据**，不属于今日计划，也不会进入任何模块。
5. 健身、饮食、游戏在第一版**不**向今日计划生成待办。
6. 待办按日期归属；昨日及更早未完成的**不自动复制**到今天，而是集中在今日计划顶部「之前未完成」折叠区，由用户决定【加入今天 / 完成掉 / 删除】。

## 7. 本地数据文件、备份和恢复要求

- **运行与存储**：单个 HTML 文件双击打开；全部数据存在这台电脑、这个浏览器的本地存储（localStorage）里。不联网、不发送任何数据。
- **保存要求**：任何增删改立即写入；刷新、关闭浏览器、重启电脑后数据不丢失（验收见第 10 节 A 组）。
- **存档结构**：全部数据是一个整体 JSON 对象 = 9 个模块的数据 + 偏好（语言、主题、上次导出时间）。导出文件与内部存档同构。
- **导出**：设置页一键导出，下载 `MyDay-YYYY-MM-DD.json`（MyDay + 导出日期，两种语言下一致）；导出后记录时间，驱动侧栏备份提醒。
- **导入**：选择存档文件 → 确认框（显示存档内各模块数据量，提示将覆盖当前全部数据）→ 覆盖导入 → 立即生效。导入与界面语言无关，中英文状态导出的文件可互相导入。
- **备份提醒**（侧栏底部）：从未导出或上次备份超过 30 天 = 红色；7~30 天 = 黄色；不足 7 天 = 绿色。
- **恢复要求**：导入后数据完整恢复，且刷新/重启后仍保持；导入失败（文件损坏、非 JSON、结构不对）时给出提示，**现有数据不受影响**。
- **风险说明（需在设置页写明）**：数据存在浏览器里，清理浏览器数据会丢失存档，建议定期导出并另存到 U 盘/网盘。

## 8. 第一版必须完成的功能

**全局**：左侧导航与首页回跳、即时自动保存、深浅色主题、中英双语（枚举存键、按语言渲染）、第 2 节的 7 条全局交互约定、侧栏备份提醒。

**分模块**：
1. 今日计划：增删改勾、归属标签、分四组、之前未完成区（三操作）、显示已完成开关；
2. 自媒体：灵感库（增删、转正式内容）、四列看板（推进、编辑、删除）、本月统计与平台分布、已发布折叠；
3. 开发工作：项目增删改名与状态、项目任务清单、笔记增删改、跨项目笔记搜索；
4. 咨询工作：客户增删改与搜索、基本信息编辑、咨询记录增删、待办一键加入今日计划、本月统计；
5. 健身计划：周模板编辑、今日训练（模板展示 + 动作记录）、补记、历史列表、本月/本周统计；
6. 饮食计划：日期回切补记、四餐记录、速查表（增删、点选带入）、喝水打卡与目标、今日热量小计；
7. 游戏娱乐：游戏增删与状态、状态筛选、记录游玩时长、累计与最近游玩、本月统计；
8. 数据与设置：语言切换、主题切换、导出、导入（确认+覆盖）、清空（输入"清空"确认）、备份提醒。

## 9. 第一版暂时不做的功能

以下均推迟到后续版本，第一版不实现：
- 提醒和通知（本地网页做系统级提醒不可靠）；
- 图表和趋势曲线（第一版只有数字统计）；
- 日历/月视图、番茄钟计时器；
- 富文本、图片、附件（第一版纯文字）；
- 周期性自动任务（先用健身周模板和"顺延"机制顶着）；
- 从其他 App 导入数据、多设备同步、手机端（按需求明确不做）。

## 10. 验收标准（可实际逐条检查）

**A. 运行与持久化**
1. 断网状态下双击 HTML 文件可打开，全部功能可正常使用（全程无任何联网请求）。
2. 添加一条今日待办 → 完全关闭浏览器 → 重新打开：该待办仍在。
3. 在任意模块修改数据后刷新页面：回到首页总览，所有数据与刷新前一致。
4. 把电脑日期改为次日再打开：昨日未完成待办不出现在今日列表，而是出现在「之前未完成」区；今日计划为新的空一天。

**B. 功能抽检**
5. 首页勾选一条待办 → 到「今日计划」看到同样已勾选；首页快速添加 → 今日计划出现该条且归属正确。
6. 今日计划添加时可选归属，四组分组正确；删除一条后 5 秒内点「撤销」可恢复。
7. 自媒体：灵感「转成正式内容」后出现在看板"想法"列；卡片点「推进→」逐列前进；点击卡片可改标题/平台/日期；本月统计数字正确。
8. 咨询：添加客户和记录后统计正确；一条记录点「加入今日计划」→ 今日计划出现归属=咨询的同名待办，且该按钮变为"已加入"、不能重复加入。
9. 健身：设置当天模板文字 → 今日训练页显示该主题；记录一条动作 → 本周/本月统计各 +1，历史列表出现当天记录。
10. 饮食：记录食物并填热量 → 今日总热量正确；从速查表点选 → 名称和热量自动带入；喝水 +1/−1 正确、目标可改。
11. 游戏：记录游玩 30 分钟 → 该游戏累计 +30、最近游玩出现一条、本月总时长 +30；状态筛选有效。

**C. 双语**
12. 设置里切到 English → 界面文字全部变英文、立即生效，无需刷新；重启后仍是英文。
13. 切换语言前后，已有数据的分类/状态显示正确（如"进行中"↔ Active、"想法"↔ Idea），数据无错乱；用户输入的内容保持原文。

**D. 备份与恢复**
14. 全新状态（从未导出）侧栏备份提醒为红色；导出后变绿色且时间正确；把系统日期调后 8 天 → 黄色。
15. 导出得到 `MyDay-YYYY-MM-DD.json`；清空全部数据（需手动输入"清空"才能执行）→ 导入该文件 → 九个模块数据完整恢复，刷新后仍保持。
16. 导入一个损坏/非 JSON 文件 → 提示导入失败，当前数据不受影响。

**E. 外观**
17. 深色/浅色切换立即生效，重启后记住选择。

---

*以上共 17 条验收标准，全部通过即第一版交付。*
